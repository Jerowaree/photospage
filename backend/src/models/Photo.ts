/**
 * MODELO MIGRADO A PRISMA
 *
 * El modelo Photo ya no se define aquí con Mongoose.
 * El esquema de la base de datos está en:
 *
 *   backend/prisma/schema.prisma  (modelo `Photo`)
 *
 * Para generar el cliente Prisma después de cambios en el schema:
 *   npm run db:generate   -- genera el cliente TypeScript
 *   npm run db:push       -- sincroniza el schema con la BD (sin historial de migraciones)
 *   npm run db:migrate    -- crea una migración SQL con historial (recomendado en producción)
 */

export { }; // módulo vacío — evita error TS si se importa por error

